# Optional: browser calls this base URL for API (production). Leave empty to use same-origin /api.
VITE_API_BASE_URL=

# Dev only: where Vite proxies /api (default http://127.0.0.1:4000)
# VITE_API_PROXY_TARGET=http://127.0.0.1:4000
